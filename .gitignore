# Vendor SDK — redistributed archive, not our source
sdk/
*.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# colcon
build/
install/
log/

# Model weights (fetched, not committed)
models/*.caffemodel
models/*.onnx
models/*.weights

# Generated audio assets
assets/audio/*.wav
assets/audio/*.pcm

# Secrets
.env
.superpowers/

# Built by tools/deployer/build.py. Rebuilt per platform, never committed:
# a 79 MB binary in git history is 79 MB in every clone, forever.
/X2部署工具
/X2部署工具.exe
tools/deployer/build/
